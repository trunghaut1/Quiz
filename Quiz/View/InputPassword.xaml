<Window x:Class="Quiz.View.InputPassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputPassword" Height="150" Width="300" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Image Source="/Quiz;component/Images/password.png" Width="32" Height="32" Grid.RowSpan="2" Margin="20,0" />

            <Label Name="lblQuestion" Grid.Column="1">Mật khẩu:</Label>
            <PasswordBox Name="txtPassword" Grid.Column="1" Grid.Row="1"></PasswordBox>

            <WrapPanel Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button IsDefault="True" Name="btnDialogOk" Click="btnDialogOk_Click" MinWidth="60" Margin="0,0,10,0">Chấp nhận</Button>
                <Button IsCancel="True" MinWidth="60">Hủy</Button>
            </WrapPanel>
        </Grid>

    </Grid>
</Window>
